{
  "version": 2,
  "builds": [
    {
      "src": "Dockerfile",
      "use": "docker"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/$1"  // Adjust this if your Docker container has a specific routing setup
    }
  ]
}
